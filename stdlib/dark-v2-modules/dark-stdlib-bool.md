# dark/stdlib/Bool



```text
// same as V1
Bool::and(Bool: a, Bool: b) -> Bool
Bool::not(Bool: b) -> Bool
Bool::or(Bool: a, Bool: b) -> Bool
Bool::xor(Bool: a, Bool: b) -> Bool

// removed
# Bool::isNull(Any check) -> Bool // no nulls anymore (also, shouldn't have been in the bool namespace)

```

